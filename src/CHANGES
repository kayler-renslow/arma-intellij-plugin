plugin
	none

shared between languages
	none

SQF
	auto completion for call and spawn (will fetch config functions)
	added documentation lookup for said config functions in auto completion
	fixed issue where \n was being removed from block comments at the start of the comment

Header
    ability to collect all of the mission's config functions