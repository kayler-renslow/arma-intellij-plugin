plugin
	stringtable reading xml api
	fixed bug where module couldn't be fetched because the given psiFile's virtual file was null

shared between languages
	documentation method for stringtable keys

SQF
	none

Header
    annotator that checks if a class is already defined in configs
    autocompletion for stringtable entries
