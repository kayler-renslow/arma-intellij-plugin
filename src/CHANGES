plugin
	removed duplicate find file code

shared between languages
	fixed stringtable documentation lookup in sqf for when there was no text

SQF
	inserting new function action works, but is buggy

Header
    none
