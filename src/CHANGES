>gave auto completetion for variables and commands icons
>Got SQF global variables to find all instances of the variable when doing "find usages"
>for SQF variables in private decl, vars that are already declared private in current scope and annotated