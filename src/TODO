>add @see functionality for documentation that will create a link to a psi element (including psi files)


To fill the completion list for named element references, the IDE calls PsiReference.getVariants() (use this instead of getReferences)
	http://www.jetbrains.org/intellij/sdk/docs/reference_guide/custom_language_support/code_completion.html