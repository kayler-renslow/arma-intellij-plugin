features:
    add tag renaming for functions
    check if function exists when doing call or spawn
    renaming files will rename them in includes



fix this scenario
_i = 100;
for [{private _i = 0}, {_i < 5}, {_i = _i + 1}] do {};
hint str _i; // 100